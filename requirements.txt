# Dependensi inti untuk eksperimen klasifikasi teks (4 label).
#
# PENTING: jangan langsung `pip install -r requirements.txt` di server yang
# memakai GPU NVIDIA RTX 50-series (Blackwell, mis. RTX 5060 Ti). Install torch
# versi CUDA dulu, baru sisanya:
#
#     pip install torch --index-url https://download.pytorch.org/whl/cu128
#     pip install -r requirements.txt
#
# Baris `torch` di bawah sengaja TANPA versi, karena versi CUDA-nya ditentukan
# oleh langkah install di atas (cu128/cu130), bukan oleh file ini.
torch
transformers
accelerate
pandas
numpy
scikit-learn
matplotlib
seaborn
huggingface_hub
# SentenceTransformer untuk pemilihan contoh few-shot berbasis similaritas semantik
# (get_example/generate_examples_bert.py). Dipin ke 3.0.0 karena API
# `similarity_fn_name` + `model.similarity()` di versi ini yang dipakai (sama seperti
# reference/ Alvaro); versi lebih baru mengubah API tersebut.
sentence-transformers==3.0.0
